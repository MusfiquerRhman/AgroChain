import { makeStyles } from "@mui/styles"

const style = makeStyles(() => ({
    heading: {
        marginTop: "2rem",
        marginBottom: '2rem',

    },
    addButton: {
        alignItems: "center",
        justifyContent: "center",
        margin: "0 auto"
    }
}))

export default style;